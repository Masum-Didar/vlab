<% form_url = @equipment.persisted? ? admin_equipment_path(@equipment) : admin_equipments_path %>

<%= form_with model: @equipment, url: form_url, class: "d-flex flex-column gap-3" do |f| %>
  <% if @equipment.errors.any? %>
    <div class="alert alert-danger">
      <strong>Please fix <%= pluralize(@equipment.errors.count, "issue") %>.</strong>
      <ul class="mb-0 mt-2">
        <% @equipment.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.label :name, class: "form-label fw-medium" %>
    <%= f.text_field :name, class: "form-control" %>
  </div>

  <div>
    <%= f.label :equipment_type, "Equipment type", class: "form-label fw-medium" %>
    <%= f.text_field :equipment_type, class: "form-control", placeholder: "Microscope, pipette, beaker" %>
  </div>

  <div>
    <%= f.label :description, class: "form-label fw-medium" %>
    <%= f.text_area :description, rows: 3, class: "form-control" %>
  </div>

  <div>
    <%= f.label :image, "Equipment image", class: "form-label fw-medium" %>
    <%= f.file_field :image, accept: "image/png,image/jpeg,image/webp,image/gif", class: "form-control" %>
    <% if @equipment.image.attached? %>
      <div class="mt-2">
        <%= image_tag @equipment.image, alt: @equipment.name, class: "img-thumbnail", style: "max-width: 180px; max-height: 140px;" %>
      </div>
    <% end %>
  </div>

  <%= f.submit class: "btn btn-primary" %>
<% end %>
